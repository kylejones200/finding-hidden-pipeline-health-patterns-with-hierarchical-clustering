Original visualization scripts from the repository root before the `src/` layout.

Use the package CLI instead:

```bash
uv run pipeline-clustering-run pipeline-health   # replaces 23_pipeline_visualizations.py
uv run pipeline-clustering-run three-projects    # replaces 25_dendrogram_visualizations.py
```
